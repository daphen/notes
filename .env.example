# Database (required)
# Use Vercel's Neon integration for automatic setup, or get a free database at https://neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Authentication (required)
# Choose any password for logging into your notes app
AUTH_PASSWORD=your-password-here

# JWT Secret (optional)
# Auto-generated from DATABASE_URL + AUTH_PASSWORD if not set
# Only set this if you need a specific secret
# JWT_SECRET=your-32-character-minimum-secret
